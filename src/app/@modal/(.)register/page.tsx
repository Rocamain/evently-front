import RegisterForm from '@/ui/auth/RegisterForm'
export const metadata = {
  title: 'Evently Register',
  description: 'Join Evently',
}

export default function SignInModal() {
  return <RegisterForm />
}
